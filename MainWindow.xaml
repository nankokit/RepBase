<Window x:Class="RepBase.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RepBase"
        mc:Ignorable="d"
        Title="RepBase"
        WindowState="Maximized"
        ResizeMode="NoResize">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="5" Background="#f0f0f0">
            <StackPanel Margin="5">
                <TextBlock Text="Таблицы" FontWeight="Bold" FontSize="16" Margin="5"/>

                <ScrollViewer Height="252" Margin="5">
                    <ItemsControl ItemsSource="{Binding TableItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding TableName}" Click="TableButton_Click" Margin="5" Background="#889C9B"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <Button x:Name="btnCreateTable" Content="создать новую таблицу" Margin="5" />
                <Button x:Name="btnDropTable" Content="удалить таблицу" Margin="5" />
            </StackPanel>

            <StackPanel Margin="5">
                <TextBlock Text="Содержание таблицы" FontWeight="Bold" FontSize="16" Margin="5" />
                <Button x:Name="btnAddRow" Content="добавить строку" Margin="5" Click="btnAddRow_Click" />
                <Button x:Name="btnEditRow" Content="изменить строку" Margin="5" />
                <Button x:Name="btnDeleteRow" Content="удалить строку" Margin="5" />
            </StackPanel>
            <StackPanel Margin="5">
                <TextBlock Text="Скрипты" FontWeight="Bold" FontSize="16" Margin="5"/>
                <ComboBox x:Name="scriptsComboBox" Margin="5"/>
                <Button Content="выполнить" Margin="5"/>
            </StackPanel>
            <StackPanel Margin="5">
                <TextBlock Text="Резервное сохранение" FontWeight="Bold" FontSize="16" Margin="5"/>
                <Button Content="создать бэкап" Margin="5"/>
            </StackPanel>
        </StackPanel>

        <DataGrid x:Name="dataGrid" Grid.Column="1" Margin="5" 
          AutoGenerateColumns="False" 
          HeadersVisibility="Column" 
          RowHeaderWidth="0" 
          HorizontalScrollBarVisibility="Auto"
          VerticalScrollBarVisibility="Auto"
          CanUserAddRows="False"
          CanUserDeleteRows="True"/>
    </Grid>
</Window>